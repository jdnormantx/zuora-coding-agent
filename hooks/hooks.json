{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "timeout": 5,
            "command": "node -e \"let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{let p='';try{p=(JSON.parse(d).tool_input||{}).file_path||''}catch(e){}if(/\\.workflow\\.json$/i.test(p)){console.error('Use bin/workflow-engine build — do not hand-write .workflow.json');process.exit(2)}process.exit(0)})\""
          }
        ]
      }
    ]
  }
}
